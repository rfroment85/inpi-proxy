{
  "name": "inpi-proxy",
  "version": "1.0.0",
  "description": "Proxy INPI pour Financiel Vision — Cabinet ACC",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
